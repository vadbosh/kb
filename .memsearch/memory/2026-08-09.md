# 2026-08-09

## Session 17:41

### 17:41
<!-- session:ses_018637b8dffeIJslOExvMMsY0G turn:msg_fe79c84a1001uIg27orDfJEsbt db:/root/.local/share/opencode/opencode.db -->
=== Turn 1 (msg_fe79c84a1001uIg27orDfJEsbt) ===
[User]: /model

[Assistant]: No task given. Wat you need — model info? code help? Say more.

### 17:41
<!-- session:ses_018637b8dffeIJslOExvMMsY0G turn:msg_fe79cfa3c001aahJtD7edqWuos db:/root/.local/share/opencode/opencode.db -->
=== Turn 2 (msg_fe79cfa3c001aahJtD7edqWuos) ===
[User]: ...(truncated to tail)
nothing written down
anywhere — that is the opposite of nothing to save. Looking means `ls` in that
stream's own directory, not a search of the filesystem: a note outside its
`kb/` is not its record.

**A named command is a request for that command, not for a session ritual.** Run
it, report what it said, stop. Do not load a reference file, do not read the
notes, do not save anything. Two exceptions, because the exit code demands an
answer:

- **exit 4 from `check`** — a credential is in a note. Report the file and line;
  do not edit it silently. `references/save.md` says why.
- **exit 3 from `check`** — act on what it lists in the same turn: `kb sync` for a
  stale index, fix or restore a missing link target, add front matter. A size
  finding is an invitation to run `kb outline` and judge, not an order to split.

With no directory named, run it for the current project; `kb list` covers every
known one.

## Tool

The CLI ships **inside this skill** — nothing depends on `PATH` or on
`~/.local/bin`, which does not exist on Windows. Your base directory is given at
the top of this skill; the binary sits in `scripts/` next to this file:

```
"<skill-dir>/scripts/kb" <command>         Linux / macOS
"<skill-dir>\scripts\kb.cmd" <command>     Windows
```

Written as `kb <command>` from here on for readability. If `kb` is also on PATH
(the installer offers that for manual use) either form works.

Requires Python 3.10+ and nothing else.

```
kb status                                     where the kb is, what is in it
kb check                                      drift (exit 3), credentials (exit 4)
kb verify                                     advisory: dead paths, stale notes
kb sync                                       rebuild the index table
kb add <slug> --kind <kind> --title "<...>"   new note + front matter + reindex
kb outline [file]                             section map — where the seams are
kb list                                       every kb known on this machine
kb streams                                    directories this session touched
```

Resolve the notes directory from cwd (`./kb`). Not the project dir →
`--dir <path>`. No `kb/` yet → `kb add` creates it, no `init` needed.

**One kb covers one work stream, and a session often touches several.** Every
other command answers for a single directory, so running one and stopping
silently decides the session had one subject. `kb streams` answers the other
question — which directories came up — by reading the transcript rather than
trusting recall.

## The one rule both halves share

**Never edit between `<!-- kb:begin -->` and `<!-- kb:end -->`.** That table is
generated; the next `kb sync` overwrites it. Change front matter instead.
Everything outside the markers was written by a human — do not rewrite it unless
asked.


Base directory for this skill: /root/.config/opencode/skills/kb
Relative paths in this skill (e.g., scripts/, references/) are relative to this base directory.

restore

[Assistant]: ## kb: /home/kb/kb   (2 файла, `state`-среза нет)

## Состояние
`status`/`check`/`verify` — чисто: индекс актуален, кредов нет, все пути резолвятся. Снимка `state` нет, поэтому «ситуация сейчас» из заметок не восстанавливается — только два тематических файла.

## Что читать под задачу
- `01-guards-beat-prose.md` — decision: почему запреты переехали из инструкций в сам бинарь
- `02-llm-failure-modes.md` — recipe: как ассистент ломает kb и что каждый раз это останавливало

Раздел «Зачем этот каталог» в overview пустой — заготовка, границы стрима не описаны.

Задача не названа — скажи, что делаем, тогда открою нужный файл.

В конце работы `/kb save` запишет, что выяснилось.

### 17:43
<!-- session:ses_018637b8dffeIJslOExvMMsY0G turn:msg_fe79db919001gHI7vgHh0AECuR db:/root/.local/share/opencode/opencode.db -->
=== Turn 3 (msg_fe79db919001gHI7vgHh0AECuR) ===
[User]: qrestore kb

[Assistant]: ## qrestore: `kb` (3 записи, автор vadym, cwd `/home/kb` — совпадает)

**session-summary (2026-08-09)** — kb 4.0.2: 62 коммита, 18 тегов от v2.0.0. `CHANGELOG.md` держит порядок выпуска, `release.sh check` сверяет версию в SKILL.md ↔ раздел журнала ↔ тег; новые теги несут раздел журнала. Установлен в три IDE, канон `ai-config` без расхождений. Заметки о разработке лежат в `/home/kb/kb` намеренно вне репозитория — `kb/` в `.git/info/exclude`.

**decision (2026-08-09)** — запреты перенесены из инструкций в бинарь. Четыре текстовые правки ассистент обошёл разными формулировками одного вывода («ничего нового», «ничего не изменилось», `find /` вместо двух `ls`, разворачивание заметок в `$HOME`). Пятая попытка была кодом: отказ разворачивать в `$HOME`/PATH/системных корнях, запись и чтение ограничены текущим каталогом. Обход для скриптов — `KB_ALLOW_OUTSIDE=1`. Вывод: правило, обходимое формулировкой, надо переносить в проверку.

**gotcha (2026-08-09)** — режимы отказа ассистента на kb (две оболочки): судит по памяти вместо диска; придумывает механизм вместо факта (пропала заметка → «внешняя очистка», на деле удалил человек); уходит искать за пределы задачи; рассуждает о предмете там, где решает каталог сессии. Останавливал не текст, а отказ инструмента.

Файлы: `/home/kb/CHANGELOG.md`, `/home/kb/release.sh`, `kb/01-guards-beat-prose.md`, `kb/02-llm-failure-modes.md`.

Записей, отменённых `supersedes`, нет. Что делаем?

