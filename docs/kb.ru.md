# kb — мануал

Хранилище знаний в Markdown на одну линию работы: указатель `00-overview.md` и
пронумерованные файлы `NN-slug.md`. Весь замысел держится на двух свойствах:

- **таблица указателя генерируется** из front matter файлов, поэтому не может
  разойтись с тем, что описывает
- **каждая заметка типизирована по способу устаревания** — `state` и `plan`
  вытесняются более новыми, `reference` и `recipe` правятся на месте, а
  `decision` не устаревает вообще, потому что фиксирует, почему так решили

Зачем это нужно и чего инструмент делать не станет — в
[README.RU.md](../README.RU.md). Этот файл — рабочий мануал: как этим управлять
каждый день и что именно гарантирует каждый механизм.

| Слой | За что отвечает | Где живёт |
|---|---|---|
| Skill `kb` | суждение — что писать, куда, что читать | каталог скиллов ассистента |
| CLI `kb` | механика — нумерация, таблица, поиск расхождений | `<skill-dir>/scripts/kb` |

Скилл подгружает одну половину по надобности: `references/save.md` на запись,
`references/restore.md` на чтение. Команда одна — `/kb`, половина выбирается
аргументом: `/kb save` и `/kb restore`.

Третий файл, `references/entry-point.md`, подгружается по более узкому условию:
команда напечатала `⚠` про `AGENTS.md` и назвала этот файл рядом. Что делать с
такими находками — страница инструкций, бесполезная на тех сохранениях, где с
точкой входа всё в порядке, а таких большинство.

Обычная работа shell не трогает — скилл сам зовёт CLI. Единственное штатное
исключение: `kb adopt`, однократно на каждый переносимый каталог.

Установка, настройки и удаление — в [README.RU.md](../README.RU.md#установка).
Работа из терминала без ассистента — в [cli.ru.md](cli.ru.md).

---

## Рабочий цикл — что набирать и когда

### Сессия 1, новый проект

Готовить заранее нечего, но и само ничего не появится: **запуск ассистента не
создаёт `kb/`**. Каталог возникает в тот момент, когда ты первый раз набираешь
`/kb save`. До этого его в проекте просто нет.

```
$ cd /path/to/project
$ claude
```

Работаешь как обычно, kb не участвует:

```
> разберись почему fluent-bit не доезжает до базы
```

Появилось что сохранить:

```
> /kb save
```

Дальше скилл всё делает сам: смотрит `kb status` (пусто — значит создаст `kb/`),
выбирает `kind`, формулирует `title`, пишет файл, пересобирает индекс. И
отчитывается счётчиками:

```
kb: создан /path/to/project/kb, +1 reference (01-fluentbit-pipeline.md). Индекс собран.
```

`/kb save` без уточнений означает «сам реши, что из этой сессии стоит
сохранить». Тему можно задать явно: `/kb save про конвейер fluent-bit`.

Следующая задача, следующий результат, снова сохраняем:

```
> почему terraform init падает с text file busy
...
> /kb save
```

Это известная проблема, значит `kind: recipe`. Такого файла ещё нет — заведёт
новый:

```
kb: +1 recipe (02-traps-and-recipes.md). Индекс собран.
```

Третью такую проблему скилл **дописал бы в тот же** `02-traps-and-recipes.md`, а
не стал бы плодить файлы. Так и выглядит выбор «дописать или завести новый».

Выход из ассистента — `/exit`.

### Сессия 2, возврат

```
$ claude --resume <ID>
```

Нужен предыдущий срез:

```
> /kb restore
```

Скилл делает `kb status` + `kb check` + `kb verify`, читает указатель и
актуальный `state`, и сверяет с тем, что уже в переписке. Один из трёх ответов:

| Что нашёл | Что скажет |
|---|---|
| срез тот же, `check` чист | «контекст актуален, перечитывать нечего» — токены не тратит |
| появился более новый `state` | читает его: «ситуация изменилась, вот что» |
| `check` нашёл расхождение (выход 3) | «заметки правили без `kb sync`» — читает изменённые файлы, а не таблицу |

Дальше брифинг: состояние, что открыто, какие файлы читать под задачу.

Если возобновил сразу и на диске никто ничего не менял, `/kb restore` можно не
звать — всё и так в переписке. Он нужен, когда **прошло время** или **работала
другая сессия**.

Задача решена, сохраняем:

```
> /kb save
```

Файлы уже есть, поэтому чаще будет **дописывать**. На этом этапе обычно уместен
срез:

```
> /kb save срез состояния
```

→ `03-state-2026-08-07.md`, указатель сам пометит его актуальным, а прежний —
`⏳ (прежний срез)`.

### Шпаргалка

| Момент | Ввод |
|---|---|
| записать что выяснили | `/kb save` |
| записать конкретно про X | `/kb save про X` |
| зафиксировать состояние на дату | `/kb save срез состояния` |
| вернулся в сессию, нужен контекст | `/kb restore` |
| что вообще есть в заметках | `/kb restore` |

Команда — `/kb`, половина выбирается первым словом: `/kb save` и `/kb restore`.
Отдельных команд `/kbsave` и `/kbrestore` не существует: скилл называется `kb`.
Слова «kbsave» и «kbrestore» в обычной фразе тоже обычно срабатывают — они
перечислены в описании скилла, — но это распознавание, а не команда.

Shell руками не нужен — скилл зовёт `kb` сам. Исключение одно: `kb adopt`,
разово, для миграции старого каталога.

---

## Как записывать

```
/kb save
/kb save про проблему с terraform init
```

Скилл делает всё сам:

1. `kb status` — что уже есть
2. решает: **дописать** в существующий файл или **завести новый**
   - дописать — если это ещё один случай того же, что файл уже покрывает
   - новый — если отвечает на другой вопрос, либо это `state`/`decision`
     (точки во времени, их не дописывают)
   - делить существующий — если >~400 строк **и** секции про разное
3. выбирает `kind`, пишет `title` от лица читателя («что мне нужно», не «как
   документ называется»)
4. `kb add` → пишет тело → `kb sync` → `kb check`
5. отчитывается счётчиками: `+1 decision (04-why-x-target.md), edited 03-traps.md`

Нечего записывать → скажет `Nothing durable for kb.`, пустых файлов не создаст.

### Шесть kind

| kind | что | вытесняется новым |
|---|---|---|
| `charter` | зачем поток существует, его границы, чего сознательно не делаем | нет, правится на месте, один на kb |
| `state` | срез на дату: что сделано, что открыто | да, более поздней датой |
| `plan` | что запланировано, порядок | да, при выполнении |
| `decision` | решение и **почему** | нет, только `--supersedes` |
| `reference` | как устроено | нет, правится на месте |
| `recipe` | известные проблемы и команды проверки | нет, правится на месте |

`state` получает сегодняшнюю дату в имя автоматически.

---

## Возврат в старую сессию

```
/kb restore
```

`claude -r` восстанавливает переписку, но файлы за это время могли измениться:
правил ты, правила другая сессия, либо детали выпали при сжатии контекста. В
результате агент помнит одно состояние заметок, а на диске лежит другое.

`/kb restore` делает именно эту сверку:

1. `kb status` — какой срез актуален **сейчас**
2. `kb check` — индекс не разошёлся с файлами
3. `kb verify` — не пропали ли пути, нет ли залежавшихся заметок
4. читает указатель, читает актуальный срез
5. **сравнивает с тем, что в переписке**, и говорит прямо:
   - срез тот же, `check` чист → контекст актуален, перечитывать нечего
   - появился более новый `state` → читает его, ситуация изменилась
   - `check` сообщил о расхождении → заметки правили без `sync`, читает изменённые файлы

Потом брифинг: состояние, что открыто, какие файлы читать под задачу, какие
расхождения нашлись. Не пересказ файлов подряд, а выжимка. Остальное открывается
по мере надобности, по колонке «что нужно» в указателе.

Автоматом при старте сессии это не срабатывает — вызывается явно. Хук
`SessionStart` для этого не нужен: сверка нужна тогда, когда ты садишься за
работу, а не при каждом запуске ассистента.

---

## Как обновляется индекс

Источник правды — **front matter файлов**; таблица — производная от него.

```
kb/02-pipeline.md            kb/00-overview.md
---                          <!-- kb:begin -->
title: как устроен ...  ──┐  | что нужно | файл | тип | обновлён |
kind: reference           ├─►| как устроен ... | `02-pipeline.md` | reference | … |
updated: 2026-08-05     ──┘  <!-- kb:end -->
---
```

`kb sync` читает front matter всех `NN-*.md` и переписывает **только** текст
между маркерами. Остальное в указателе — байт в байт как было.

| Действие | Индекс |
|---|---|
| `kb add` (внутри `/kb save`) | пересобирается сразу |
| правка `title`/`kind`/`updated` | нужен `kb sync` |
| файл удалён/переименован | нужен `kb sync` |

В промежутке между правкой файла и `kb sync` индекс устаревший — это и ловит
`kb check` (`index table is stale`, выход 3). Внутри `/kb save` и `sync`, и
`check` вызываются сами.

**Считается автоматически, руками не пишется:**

- актуальный срез — max дата среди `state`, отдельной строкой над таблицей
- прежние срезы помечаются `⏳ (прежний срез)`
- если в front matter заметки стоит `supersedes:`, обе строки получают стрелку:
  заместившая — `decision ⤺ 01` в колонке типа, заменённая — `⤺ (отменён 03)`
  в описании. Заменённый файл остаётся на месте: доводы за прежний выбор обычно
  и нужны через полгода. Если он удалён, строки для него нет, а имя перестаёт
  считаться битой ссылкой
- порядок строк — по номеру файла: правка одного файла не перетасовывает таблицу
- номер нового файла — следующий свободный; освободившиеся номера повторно не
  выдаются (на номер, который когда-то принадлежал файлу, могут ещё ссылаться)

---

## Точка входа в корне проекта

Всё, что описано выше, начинается с команды `kb brief`. Человек, который про
заметки не знает, эту команду не запустит. А файл в корне проекта агент читает
всегда.

```
kb route
```

Команда `kb route` пишет в корень проекта два файла. Первый — `AGENTS.md`: в нём
указатель на заметки, charter и актуальный срез. Второй — `CLAUDE.md`, в нём одна
строка `@AGENTS.md`.

Файла два, потому что Claude Code читает `CLAUDE.md` и не читает `AGENTS.md`, а
Codex и Opencode читают `AGENTS.md`. Сам текст при этом лежит только в
`AGENTS.md`, а `CLAUDE.md` его импортирует.

Команду `kb route` вызывает и `/kb save` — когда создаёт каталог `kb/` в проекте,
где `AGENTS.md` ещё нет. Если `AGENTS.md` в проекте уже есть, `/kb save` только
предложит запустить `kb route`, а сам файл не тронет.

**Что генерируется, а что твоё.** Внутри маркеров `kb:begin` и `kb:end` лежит то,
что kb выводит из заметок; этот блок обновляют команды `kb route` и `kb sync`.
Снаружи маркеров лежит твой текст: что это за работа, команды проверки,
определение готовности. Сам CLI оставляет на их месте комментарии `kb:fill` —
прозу он писать не умеет. **Заполняет их `/kb save`, в том же ходу**: берёт
заготовку из заметок, которые только что написал, и из команд, которые сессия
действительно запускала, а потом говорит, что написал, — чтобы вы поправили.
Спрашивает он только там, где в сессии ответа нет: поток, где ничего не
запускали, не даёт честной команды проверки, а выдуманная попадёт в файл,
который агент выполнит. `AGENTS.md` без
маркеров kb не трогает и писать в него отказывается. В `CLAUDE.md` без импорта kb
тоже ничего не пишет, а печатает строку, которую надо добавить руками.

**Длина.** Оба файла попадают в контекст на старте каждой сессии, поэтому их
суммарная длина ограничена 200 строками. В эту сумму входит всё, что хост
разворачивает на старте: импорт `@path` на глубину до четырёх прыжков и
`CLAUDE.local.md` рядом с `CLAUDE.md`.

Предел считается на каталог запуска, а не на файл. Сессия, запущенная в
подкаталоге, грузит его `CLAUDE.md` поверх всех вышестоящих, поэтому `kb route`
и `kb verify` сообщают и про такие цепочки, начиная с худшей. Сокращать эти
файлы kb не станет: за маркерами лежит твой текст.

## В репозитории или только на этой машине

Заметки внутри git-репозитория по умолчанию коммитятся — этого никто не
выбирает, это делает `git add -A`. Вопрос задаётся один раз, при создании
каталога, потому что дальше он уже не возникает:

```
kb local          # .git/info/exclude — заметки остаются здесь
                  # либо закоммитить, и они поедут вместе с проектом
```

`.git/info/exclude`, а не `.gitignore`: второй сам коммитится, то есть
навязывает выбор одного человека всем, кто клонирует репозиторий.

Ответ нигде не хранится — `kb status` спрашивает git. Хранимый флаг был бы
второй копией того, что git и так знает, и разошёлся бы с ним при первой же
правке правил игнорирования.

---

## CLI — справочник

| Команда | Что |
|---|---|
| `kb status` | где kb, сколько файлов, какой срез актуален, самые крупные |
| `kb brief` | указатель и актуальный срез дословно — один и тот же вывод при одних и тех же файлах |
| `kb check` | расхождения механики; выход 3 если есть |
| `kb verify` | подозрения: пропавшие пути, залежавшиеся заметки, работа, ушедшая вперёд записей; выход 3 |
| `kb sync` | пересобрать таблицу из front matter |
| `kb add <slug> --kind <k> --title "..."` | новый файл + front matter + пересборка |
| `kb list [--scan DIR] [--prune]` | все известные kb: где лежат, что внутри |
| `kb streams [--sessions N]` | какие каталоги трогала сессия — читается из стенограммы |
| `kb outline [файл]` | карта разделов с весами — где швы для дробления |
| `kb init [--title ...]` | только скелет указателя |
| `kb adopt [--apply] [--in-place]` | миграция существующего каталога |
| `kb hook --install` | git pre-commit, отклоняет коммит при коде 4 |
| `kb route` | `AGENTS.md` в корне проекта + `CLAUDE.md`, который его импортирует |
| `kb local [--dry-run]` | оставить заметки вне git (`.git/info/exclude`) |
| `--dir X` | работать в X вместо `./kb` |

`kb check` даёт код 3 при расхождении и **код 4, если в заметке найдены учётные
данные**. Что именно проверяется и чего проверка не ловит — в
[README](../README.RU.md#секреты).

`kb check` ловит: устаревшую таблицу, ссылки на удалённые файлы, дату в `updated`,
которая датой не является, пробел в имени файла, файлы без front
matter, `.md` вне схемы `NN-slug.md`, оставшиеся `.bak`, **файлы за порогом
размера**.

### Размер файлов

Правило «не больше 200 строк» — про файлы инструкций (`CLAUDE.md`, `rules/*.md`,
`SKILL.md`), которые грузятся в контекст **всегда**. Файлы kb читаются по
выбору, поштучно, поэтому порог другой и мягче:

| Порог | Что значит |
|---|---|
| 400 строк | посмотреть, не про разное ли секции |
| 1200 строк | один `Read` обходится дорого; файл помечается в индексе `⚠ ~Nk токенов` |

**Размер сам по себе не повод дробить.** Длинный связный `reference` — хороший
файл. Настоящий критерий — когда в одном файле смешались разные `kind`: поле
`kind` как раз и означает «на какой вопрос отвечает файл». Судить по `kb outline`:
карта из десятка заголовков вместо чтения полутора тысяч строк.

Автоматического дробления нет и не будет: где резать — вопрос смысла. Но и следить
за этим вручную не надо — `/kb save` вызывает `kb check` в конце каждой записи, и скилл
обязан отреагировать на предупреждение о размере в том же ходе.

### Проверка на соответствие реальности

`check` сверяет индекс с файлами — **внутреннюю согласованность**, а не
соответствие реальности. За это отвечает отдельная команда:

```bash
kb verify
```

`verify` даёт **подозрения**, а не дефекты, поэтому и вынесена из `check`.
`check` однозначен и механичен: выход 3 значит «устранить». `verify` работает
эвристикой: выход 3 значит «проверить глазами». Объединять их нельзя — точный
сигнал растворится в предположениях.

**Пути.** Проверяется путь, только если выполнены оба условия: он взят в
обратные кавычки (`` `так` ``) и его корень — первые два компонента — существует
на диске. Замер на живом kb: без этих условий выходит 271 флаг на 296 путей, из
них настоящих около двух — URL-адреса (`/stats/prometheus`), версии API
(`/v1alpha1`), обрубки длинных путей. С условиями — 6 флагов, 2 настоящих.
Обратная сторона: путь внутри блока кода, без кавычек, не проверяется. Осознанный
компромисс — при обратном соотношении отчёт быстро перестают читать.

Путь вида `~/…` считается абсолютным и разворачивается перед проверкой. Сначала
его не брали — казалось, это синтаксис оболочки, а не путь, — и так проверку
обходила ровно та запись, которой люди обозначают файл в своём домашнем каталоге.

Пути ищутся в заметках, в обзоре и в точке входа — `AGENTS.md`, `CLAUDE.md` и
всё, что они импортируют. Точку входа агент читает раньше, чем трогает проект, а
в репозитории, где артефакты ассистентов исключены из git, её не видит и
коммит-хук.

**Работа ушла вперёд заметок.** kb берёт время самого свежего файла в каталоге
работы и сравнивает с самым свежим файлом в `kb/`. Разрыв больше часа значит:
что-то делали и не записали.

Git тут не используется — больше половины потоков не репозитории.

Работой не считается:

| Что пропускаем | Почему |
|---|---|
| сам каталог `kb/` | это запись о работе, а не работа |
| `AGENTS.md` и `CLAUDE.md` в корне | их пишет kb |
| вложенный kb — `<каталог>/kb/00-overview.md` или обзор россыпью | это соседний поток |
| каталоги из `.gitignore` | это выход сборки |

**Возраст по kind.** Не «неправда», а «сюда давно не смотрели»:

| kind | порог |
|---|---|
| `recipe` | 120 дней — устаревает вслед за инструментами |
| `reference` | 180 дней |
| `charter` | 365 дней — направление должно переживать релиз; флажок раньше приучил бы пропускать весь отчёт |
| `decision` | **не проверяется** — «почему так решили» остаётся правдой всегда |
| `state`, `plan` | не проверяются — вытеснение уже работает |

**Чего `verify` не умеет**: подтвердить утверждение о живой системе («NLB
называется так-то», «CRD в AU нет»). Для этого нужен запрос к самой системе —
это дорого, требует доступов, и регулярно запускать `terraform`/`kubectl` на
проде никто не станет. Рассуждения («почему X-Target, а не Host») не проверяемы в
принципе.

### Реестр

`kb list` показывает все известные каталоги заметок. Реестр —
`~/.local/state/kb/registry.txt` (переопределяется `KB_REGISTRY`). Пополняется
сам при создании kb, а при чтении каждый путь проверяется — существует ли он ещё:

```bash
kb list                    # все kb: путь, файлов, актуальный срез, крупные
kb list --scan /projects       # подобрать заведённые до реестра или руками
kb list --prune            # убрать записи, у которых пропал указатель
```

Заметки всегда в `kb/`. Если `00-overview.md` лежит прямо в корне каталога,
команды `add` и `init` откажутся работать — иначе в одном каталоге окажется два
набора заметок. Обход:
`adopt --in-place`, только если снаружи ссылаются на старые пути.

---

## Чего не делать

- Не править таблицу внутри `kb:begin`/`kb:end` — её затрёт первый же `kb sync`.
- Не переписывать текст вне маркеров без нужды — там формулировки, написанные
  человеком.
- Не хранить пароли и ключи. Ссылаться на механизм (секрет в кластере,
  переменная окружения), но не на само значение.
- Не дублировать документацию, которая живёт рядом с кодом — она правится
  вместе с кодом. Достаточно указать её путь.
- Не вести дневник. `state` — срез, а не хроника «что делали во вторник».

---

## Язык

Диагностика инструмента (`check`/`list`/`outline`/`status`) — английская.
Текст, который пишется **внутрь** заметок (шапка таблицы, строка «Актуальный
срез», скелет указателя), идёт по `KB_LANG`: по умолчанию `ru`, для английского
`KB_LANG=en kb ...`. Новый язык — один ключ в словаре `STRINGS` внутри CLI,
больше ничего менять не надо.

Триггер-фразы скиллов двуязычные («запиши в kb» и «save to kb» работают оба),
тела скиллов английские — их читает LLM.
